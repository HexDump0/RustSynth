1. Add a React editor component like `monaco-editor`, `codemirror`, or `prismjs` to `App.tsx`.
2. Configure basic token highlighting mapping to the EisenScript keywords (set, rule, box, sphere, etc).
