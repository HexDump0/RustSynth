# T28 — Add syntax highlighting to Tauri app

## Outcome
Legacy GTK syntax highlighting (done in `rustsynth_app_gtk/src/app.rs`) was lost in the UI framework migration to Tauri. We need a React-based syntax highlighting component for EisenScript.

## Status
READY
