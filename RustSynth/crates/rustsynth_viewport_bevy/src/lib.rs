//! `rustsynth_viewport_bevy` — optional Bevy viewport backend (skeleton).
//!
//! Implements `rustsynth_render_api::ViewportBackend` using Bevy.
//! Bevy dependency is commented out until T15 begins.

pub mod backend;
