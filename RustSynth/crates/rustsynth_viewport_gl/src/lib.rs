//! `rustsynth_viewport_gl` — optional custom OpenGL viewport backend (skeleton).
//!
//! Implements `rustsynth_render_api::ViewportBackend` using a custom OpenGL renderer.
//! OpenGL bindings are commented out until T16 begins.

pub mod backend;
